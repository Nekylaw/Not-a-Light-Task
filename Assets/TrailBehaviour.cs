using UnityEngine;

public class TrailBehaviour : MonoBehaviour
{
    public GameObject center;

    // Update is called once per frame
    void Update()
    {
        //transform.RotateAround(center.transform.position, Vector3.right, 180);
    }
}
